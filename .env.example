# Copy this file to .env and fill in the real value. Never commit .env.
# The database connection string for the team Supabase Postgres instance.
# Get the current value from the team's private channel, not from email,
# and rotate the password if it has ever been shared in plain text.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/postgres
